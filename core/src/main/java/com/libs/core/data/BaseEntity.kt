package com.libs.core.data

/**
 * The base entity interface that all entities should extends this.
 * That's meaning the base abstract entity impl this interface.
 * This entity which impl this interface shall could cached by sql.
 */
interface BaseEntity
